/*
 * Spawner logic
 */
const u = require('utils');

const Spawner = {

  should_spawn(spawner) {
    console.log(`${u.name(spawner)} not implemented.`);
    return false;
  },

};

module.exports = Spawner;
