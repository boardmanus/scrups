/**
 * Allows to harvest a mineral deposit.
 *
 * @class
 * @extends {OwnedStructure}
 */
StructureExtractor = function() { };

StructureExtractor.prototype = _.extend(Object.create(OwnedStructure.prototype),
  {

  });
